const currencyAmountValueSelector = (state) => state.elements.amountInputValue;
const fromInputValueSelector = (state) => state.elements.fromInputValue;
const toInputValueSelector = (state) => state.elements.toInputValue;
export { currencyAmountValueSelector, fromInputValueSelector, toInputValueSelector, };
